interface:
  display_name: "Execute Issues Parallel"
  short_description: "Run multiple GitHub issues in safe parallel lanes"
  default_prompt: "Use $execute-issues-parallel to execute these issues in parallel with separate worktrees and PRs."
